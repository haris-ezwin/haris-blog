---
import { identity } from "../config";
import type { SEOInfo } from "../config";

interface Props {
  seo: SEOInfo;
  navVariant?: "home" | "post";
}

const { seo, navVariant = "home" } = Astro.props;
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{seo.title}</title>
    <meta name="description" content={seo.description} />
    <meta property="og:title" content={seo.title} />
    <meta property="og:description" content={seo.description} />
    <meta property="og:image" content={seo.image} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Instrument+Serif:ital@0;1&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="page-shell">
      {
        navVariant === "post" && (
          <nav class="post-nav">
            <a href="/">← {identity.handle}</a>
          </nav>
        )
      }

      <slot />

      <footer class="site-footer">
        <span>© 2026 {identity.name}</span>
        <span>Made in {identity.location}</span>
      </footer>
    </div>
  </body>
</html>

<style is:global>
  @font-face {
    font-family: "Inter";
    src: url("/fonts/InterVariable.woff2") format("woff2");
    font-weight: 100 900;
    font-style: normal;
    font-display: swap;
  }

  :root {
    --paper: #fbfbf8;
    --ink: #171714;
    --muted: #4d4d48;
    --quiet: #8a8a83;
    --line: #dfdfd8;
    --fg: var(--ink);
    --faint: var(--quiet);
    --accent: #2f6554;
    --hover: #f2f2ed;
    --card: #f3f3ee;
  }

  * {
    box-sizing: border-box;
  }

  html {
    min-width: 320px;
    background: var(--paper);
  }

  body {
    margin: 0;
    color: var(--ink);
    background:
      radial-gradient(circle at 36% 0%, rgba(255, 214, 231, 0.52), transparent 21rem),
      radial-gradient(circle at 67% 2%, rgba(201, 231, 255, 0.52), transparent 22rem),
      radial-gradient(circle at 82% 4%, rgba(220, 247, 207, 0.42), transparent 18rem),
      var(--paper);
    font-family: "Inter", Arial, sans-serif;
    -webkit-font-smoothing: antialiased;
  }

  a {
    color: inherit;
    text-underline-offset: 3px;
  }

  img {
    max-width: 100%;
  }

  .page-shell {
    min-height: 100vh;
  }

  .post-nav,
  .site-footer {
    width: min(calc(100% - 48px), 422px);
    margin-inline: auto;
  }

  .post-nav {
    padding-top: 36px;
    font-size: 12px;
  }

  .post-nav a {
    text-decoration: none;
  }

  .site-footer {
    display: flex;
    justify-content: space-between;
    gap: 20px;
    padding: 20px 0 54px;
    border-top: 1px solid var(--line);
    color: var(--quiet);
    font-size: 10px;
  }

  @media (max-width: 540px) {
    .post-nav,
    .site-footer {
      width: calc(100% - 44px);
    }

    .site-footer {
      padding-bottom: 36px;
    }
  }
</style>
